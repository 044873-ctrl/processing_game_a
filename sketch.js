let canvasW=800,canvasH=600,ball,zoneY,zoneX,zoneW,batBaseX,batBaseY,swing=false,swingProgress=0,swingDuration=12,batAngle=-0.6,score=0,outs=0,judgment='',judgmentTimer=0,pitchDelay=60,pitchTimer=0,gameOver=false
function initPositions(){zoneY=height-140;zoneW=160;zoneX=width/2;batBaseX=width/2+60;batBaseY=height-80}
function spawnBall(){let t=random();let startX=width/2+random(-20,20);let vx=0;let vy=2+random(0,3);let ay=0.4;let state='pitch';let r=10;if(t<0.7){vx=random(-0.5,0.5);startX=width/2+random(-20,20)}else{let side=random()<0.5?-1:1;startX=width/2+side*(zoneW/2+random(80,160));vx=side*random(2,5);vy=2+random(0,2)}ball={x:startX,y:80,r:r,vy:vy,state:state,resolved:false,vx:vx,ay:ay,enteredZone:false}}
function updateSwing(){if(swing){swingProgress++;let t=swingProgress/swingDuration;if(t<1){let eased=-0.5*Math.cos(PI*t)+0.5;batAngle=lerp(-0.6,1.0,eased)}else{batAngle=-0.6;swing=false;swingProgress=0}if(swingProgress===1){evaluateHit()}}}
function evaluateHit(){if(ball.state==='pitch'&&!ball.resolved){let dx=abs(ball.x-zoneX);let dy=abs(ball.y-zoneY);let inZone=dx<=zoneW/2;if(inZone&&dy<=10){ball.state='air';ball.vx=random(-6,6);ball.vy=-14-random(0,6);ball.ay=0.25;ball.resolved=true;score+=4;judgment='HOMERUN!';judgmentTimer=120}else if(inZone&&dy<=30){ball.state='air';ball.vx=random(-5,5);ball.vy=-10-random(0,4);ball.ay=0.35;ball.resolved=true;let add=floor(random(1,4));score+=add;judgment='HIT!';judgmentTimer=90}else if(inZone&&dy<=60){ball.state='air';ball.vx=random(-3,3);ball.vy=-7-random(0,3);ball.ay=0.4;ball.resolved=true;let add2=floor(random(1,2));score+=add2;judgment='WEAK HIT';judgmentTimer=80}else{ball.resolved=true;if(inZone){outs++;judgment='STRIKE!';judgmentTimer=70}else{judgment='BALL';judgmentTimer=80}}}}
function drawField(){noStroke();fill(30,120,30);rect(0,0,width,height);fill(60,140,60);ellipse(width/2,120,120,40);fill(200,180,140);ellipse(width/2,120,60,20);let baseSize=24;fill(255);quad(width/2-baseSize,height-60, width/2, height-60-baseSize/2, width/2+baseSize, height-60, width/2, height-60+baseSize/2);fill(100,60,20);rect(width/2-150,height-200,300,10);stroke(255,200,0);strokeWeight(2);noFill();rect(zoneX-zoneW/2,zoneY-20,zoneW,40,6);noStroke()}
function drawBall(){noStroke();fill(255);ellipse(ball.x,ball.y,ball.r*2,ball.r*2);stroke(200);strokeWeight(2);line(ball.x-ball.r/2,ball.y,ball.x+ball.r/2,ball.y);strokeWeight(1);noStroke()}
function drawBat(){push();translate(batBaseX,batBaseY);rotate(batAngle);noStroke();fill(100);rect(-10,-80,20,120,6);fill(150,100,30);rect(-6,40,12,40,4);pop()}
function drawUI(){noStroke();fill(255);textSize(20);textAlign(LEFT,TOP);text('Score: '+score,20,20);text('OUTS:',20,50);for(let i=0;i<3;i++){if(i<outs){fill(0);noStroke();ellipse(100+i*24,62,14,14)}else{noFill();stroke(0);strokeWeight(1);ellipse(100+i*24,62,14,14);fill(255);noStroke()}}fill(255);textAlign(CENTER,TOP);textSize(28);text(judgment,width/2,20)}
function drawGameOver(){noStroke();fill(0,150);rect(0,0,width,height);fill(255);textAlign(CENTER,CENTER);textSize(48);text('GAME OVER',width/2,height/2-40);textSize(28);text('FINAL SCORE: '+score,width/2,height/2+10);textSize(18);text('PRESS R TO RETRY',width/2,height/2+50)}
function resetGame(){score=0;outs=0;judgment='';judgmentTimer=0;gameOver=false;spawnBall();pitchTimer=0}
function keyPressed(){if(keyCode===32&&!gameOver){if(!swing){swing=true;swingProgress=0}}if((key==='r'||key==='R')&&gameOver){resetGame()}}
function mousePressed(){if(!gameOver){if(!swing){swing=true;swingProgress=0}}}
function setup(){createCanvas(canvasW,canvasH);initPositions();spawnBall();frameRate(60)}
function draw(){background(30,120,40);drawField();drawUI();if(gameOver){drawGameOver();return}if(ball.state==='pitch'){ball.y+=ball.vy;ball.x+=ball.vx;if(!ball.enteredZone&&ball.y>=zoneY-20&&ball.y<=zoneY+20){if(abs(ball.x-zoneX)<=zoneW/2){ball.enteredZone=true}}if(!ball.resolved&&ball.y>zoneY+40){ball.resolved=true;if(ball.enteredZone){outs++;judgment='STRIKE!';judgmentTimer=90}else{judgment='BALL';judgmentTimer=90}}}else if(ball.state==='air'){ball.vy+=ball.ay;ball.x+=ball.vx;ball.y+=ball.vy}if(ball.y>height+200){pitchTimer++;if(pitchTimer>pitchDelay){pitchTimer=0;spawnBall()}}updateSwing();drawBat();drawBall();if(judgmentTimer>0){judgmentTimer--;if(judgmentTimer===0){judgment=''}}if(outs>=3){gameOver=true}}
