let canvasW=640,canvasH=480;let lanes=4;let laneKeys=['d','f','j','k'];let laneX=[];let scheduledNotes=[];let activeNotes=[];let scheduleIndex=0;let bpm=120;let beatInterval=60000/bpm;let travelTime=1500;let judgeY=380;let hitWindow=150;let missWindow=300;let score=0,combo=0,maxCombo=0,hits=0,misses=0;let startTime=0;let messages=[];function setup(){createCanvas(canvasW,canvasH);frameRate(60);for(let i=0;i<lanes;i++){laneX[i]=(canvasW/(lanes+1))*(i+1);}startTime=millis();generateSchedule();}function generateSchedule(){scheduledNotes=[];scheduleIndex=0;let songBeats=120;for(let b=0;b<songBeats;b++){let beatTime=b*beatInterval;let r=Math.random();if(r<0.65){let lane=Math.floor(Math.random()*lanes);scheduledNotes.push({time:beatTime,lane:lane});}}}function draw(){background(30);let currentTime=millis()-startTime;spawnNotes(currentTime);updateNotes(currentTime);drawLanes();drawNotes();drawHUD(currentTime);drawMessages(currentTime);}function spawnNotes(currentTime){while(scheduleIndex<scheduledNotes.length){let s=scheduledNotes[scheduleIndex];if(s.time - travelTime <= currentTime){let radius=16;let startY=-radius;let distance=judgeY - startY;let vy=distance / travelTime;activeNotes.push({lane:s.lane,x:laneX[s.lane],y:startY,vy:vy,targetTime:s.time,radius:radius,spawnTime:currentTime});scheduleIndex++;}else{break;}}}function updateNotes(currentTime){for(let i=activeNotes.length-1;i>=0;i--){let n=activeNotes[i];let dt=deltaTime; if(typeof dt!=='number'){dt=16.6667;} n.y += n.vy * dt; if(currentTime - n.targetTime > missWindow){misses++;combo=0;messages.push({text:'miss',x:n.x,y:judgeY-20,start:currentTime,duration:500});activeNotes.splice(i,1);} }}function drawLanes(){noStroke();for(let i=0;i<lanes;i++){fill(50);let w=canvasW/lanes-20;rect(i*(canvasW/lanes)+10,0,w,canvasH);fill(80);rect(i*(canvasW/lanes)+10,judgeY,canvasW/lanes-20,4);fill(255);textAlign(CENTER,CENTER);textSize(16);text(laneKeys[i].toUpperCase(),laneX[i],judgeY+30);}}function drawNotes(){for(let i=0;i<activeNotes.length;i++){let n=activeNotes[i];fill(255,150,0);ellipse(n.x,n.y,n.radius*2,n.radius*2);} }function drawHUD(currentTime){fill(0,150);rect(0,0,150,80);fill(255);textSize(14);textAlign(LEFT,TOP);text('Score: '+score,10,10);text('Combo: '+combo,10,30);text('Max: '+maxCombo,10,50);let songEnd = scheduledNotes.length>0 ? scheduledNotes[scheduledNotes.length-1].time + travelTime : 1;let progress = constrain(currentTime / songEnd,0,1);fill(100);rect(160,10,400,12);fill(0,200,100);rect(160,10,400*progress,12);}function drawMessages(currentTime){textAlign(CENTER,CENTER);textSize(20);for(let i=messages.length-1;i>=0;i--){let m=messages[i];let elapsed=currentTime - m.start;if(elapsed>m.duration){messages.splice(i,1);continue;}let p=elapsed / m.duration;let alpha=255*(1-p);if(alpha<0){alpha=0;}fill(255,alpha);text(m.text,m.x,m.y - p*20);} }function keyPressed(){let k=key.toLowerCase();let lane=laneKeys.indexOf(k);if(lane!==-1){let currentTime=millis()-startTime;let bestIdx=-1;let bestDiff=hitWindow+1;for(let i=0;i<activeNotes.length;i++){let n=activeNotes[i];if(n.lane===lane){let diff=Math.abs(currentTime - n.targetTime);if(diff<bestDiff){bestDiff=diff;bestIdx=i;}}} if(bestIdx!==-1 && bestDiff<=hitWindow){let n=activeNotes[bestIdx];if(bestDiff<=75){score+=300;}else if(bestDiff<=150){score+=100;}else{score+=50;}combo++;hits++;if(combo>maxCombo){maxCombo=combo;}messages.push({text:'good!!',x:n.x,y:judgeY-20,start:currentTime,duration:600});activeNotes.splice(bestIdx,1);} else {misses++;combo=0;messages.push({text:'miss',x:laneX[lane],y:judgeY-20,start:millis()-startTime,duration:500});}}}
