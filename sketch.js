let cols=6;let rows=12;let cell=36;let canvasW=cols*cell;let canvasH=rows*cell;let grid=[];let colors=['#000000','#ff4d4d','#4dff4d','#4d4dff','#ffd24d'];let current=null;let dropInterval=30;let dropCounter=0;let fastDrop=false;let gameState='play';let score=0;let chainCount=0;let rngSeed=1;function setup(){createCanvas(canvasW,canvasH);initGrid();spawnPiece();frameRate(60);}function draw(){background(30);drawGrid();if(gameState==='play'){updatePiece();}drawHUD();}function initGrid(){grid=[];for(let r=0;r<rows;r++){let row=[];for(let c=0;c<cols;c++){row.push(0);}grid.push(row);}}function randInt(a,b){rngSeed=(rngSeed*9301+49297)%233280;let rnd=rngSeed/233280;return Math.floor(a + rnd*(b-a+1));}function spawnPiece(){let aColor=randInt(1,4);let bColor=randInt(1,4);current={a:{c:Math.floor(cols/2)-1
