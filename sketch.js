let player={};let bullets=[];let enemies=[];let particles=[];let stars=[];let score=0;let gameOver=false;let shootCooldown=0;function setup(){createCanvas(400,600);player={x:width/2,y:height-30,r:14,speed:5};for(let i=0;i<30;i++){stars.push({x:random(width),y:random(height),r:random(1,3),vy:random(0.5,2)})}}function draw(){background(0);for(let i=0;i<stars.length;i++){let s=stars[i];s.y+=s.vy;if(s.y>height){s.y=0;s.x=random(width)}fill(255);noStroke();ellipse(s.x,s.y,s.r,s.r)}if(!gameOver){if(keyIsDown(LEFT_ARROW)){player.x-=player.speed}if(keyIsDown(RIGHT_ARROW)){player.x+=player.speed}player.x=constrain(player.x,player.r,width-player.r);if(shootCooldown>0){shootCooldown--}if(keyIsDown(32)&&shootCooldown<=0){bullets.push({x:player.x,y:player.y-player.r,r:4,vy:-8});shootCooldown=8}if(frameCount%60===0){enemies.push({x:random(12,width-12),y:-12,r:12,vy:2})}for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];b.y+=b.vy;if(b.y<-b.r){bullets.splice(i,1);continue}fill(255,200,0);noStroke();ellipse(b.x,b.y,b.r*2,b.r*2)}for(let ei=enemies.length-1;ei>=0;ei--){let e=enemies[ei];e.y+=e.vy;if(e.y>height+e.r){enemies.splice(ei,1);continue}let dxp=e.x-player.x;let dyp=e.y-player.y;if(dxp*dxp+dyp*dyp<= (e.r+player.r)*(e.r+player.r)){gameOver=true}let destroyed=false;for(let bi=bullets.length-1;bi>=0;bi--){let b=bullets[bi];let dx=b.x-e.x;let dy=b.y-e.y;if(dx*dx+dy*dy<= (b.r+e.r)*(b.r+e.r)){let ex=e.x;let ey=e.y;bullets.splice(bi,1);enemies.splice(ei,1);destroyed=true;score++;for(let k=0;k<5;k++){particles.push({x:ex,y:ey,r:3,life:20,vx:random(-2,2),vy:random(-2,2)})}break}}if(!destroyed){fill(200,50,50);noStroke();ellipse(e.x,e.y,e.r*2,e.r*2)}}for(let pi=particles.length-1;pi>=0;pi--){let p=particles[pi];p.x+=p.vx;p.y+=p.vy;p.life--;if(p.life<=0){particles.splice(pi,1);continue}fill(255,150,0,map(p.life,0,20,0,255));noStroke();ellipse(p.x,p.y,p.r*2,p.r*2)}fill(0,150,255);noStroke();ellipse(player.x.player?player.x:player.x,player.y,player.r*2,player.r*2)}else{fill(100);noStroke();ellipse(player.x,player.y,player.r*2,player.r*2)}fill(255);textSize(16);textAlign(LEFT,TOP);text('Score: '+score,10,10);if(gameOver){textSize(32);textAlign(CENTER,CENTER);text('Game Over',width/2,height/2)}}
